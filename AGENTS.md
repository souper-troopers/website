# Souper Troopers Website

## Stack (decided 2026-08-08 — do not change without asking the user first)
- **Framework:** Astro
- **Interactivity:** Svelte 5, using **runes** syntax (`$state`, `$derived`, etc.) — not Svelte 4 stores or `export let` reactive statements. If unsure which syntax an example uses, default to runes.
- **Content:** Sanity (headless CMS), project `wqa0no5g` / dataset `production`. Schema lives in `studio/schemaTypes/` in this repo; content lives in Sanity's hosted dataset, fetched at build time via `src/lib/sanity.ts`. Live since 2026-08-08 — six types (`successStory`, `teamMember`, `partner`, `product`, `impactStat`, `siteSettings`), all wired into the site's pages. Structural/rarely-changing copy (CAST steps, Humanity Hub services list, hero headline) intentionally stays hardcoded in `.astro` files, not in Sanity.
- **Hosting:** Vercel or Netlify (static/JAMstack deploy, auto HTTPS/CDN, no server to maintain).
- **Shop/checkout:** ~~Stripe Checkout or Snipcart~~ **corrected 2026-08-09**: Stripe doesn't support South African merchant accounts directly (routes through Paystack, its African arm), and Snipcart has no native Paystack support (would need a fully custom gateway integration, defeating the point of using Snipcart). If real checkout is built, it's **Paystack direct** — own cart, one small piece of backend automation to hand off to Paystack's hosted payment page. See `docs/shop-checkout-decision.md` — whether to build this at all yet (vs. staying with the current manual request-and-confirm flow) is still open, pending Kerry's read on actual order volume.
- **Donations:** link out to the org's existing/new donation platform rather than building custom payment infra.

## Working agreement
- The user must be consulted before any tech/stack choice or change — do not swap frameworks, CMS, or hosting providers unilaterally.
- **Content backups (built 2026-08-09)**: `.github/workflows/sanity-backup.yml` exports the production Sanity dataset weekly and pushes it to a separate **private** repo, `souper-troopers/website-backups` — kept out of this (public) repo since exports likely contain personal data (beneficiary names/stories, contact details, unpublished drafts). Both required secrets are set on `website`: `SANITY_API_TOKEN` (Viewer/read-only) and `BACKUP_REPO_TOKEN` (fine-grained PAT, write-scoped to `website-backups` only). **Gotcha (corrected 2026-08-09 — worse than first thought)**: GitHub requires a workflow file to exist on the **default branch** before it can be run *at all* via `workflow_dispatch` — not just for the `schedule` trigger. Confirmed by trying `gh workflow run` against `dev`: 404, "workflow not found on the default branch." So this can't even be test-run until merged to `main`, not just left to activate its cron later. This also doubles as the migration path if ever needed.
- **Git workflow (added 2026-08-09, cost-driven):** do day-to-day commits on the `dev` branch, not `main`. Netlify auto-deploys `main` on every push (a production deploy, 15 Netlify credits each on the Free plan — 300/month total) and pushing every small commit straight to `main` burns through that fast. Push to `dev` freely (no production-deploy cost), then merge `dev` → `main` only when actually ready to ship a batch of changes to the live preview.

## Publish → rebuild pipeline
This is a static site (`output: "static"`) — pages are pre-rendered once at build time, so a Sanity edit alone doesn't change the live site. A Sanity webhook ("Netlify rebuild", id `IQSKUBSymrG2dmll`) POSTs to a Netlify build hook whenever a document changes in `production`, which triggers a fresh `astro build` and redeploy. Verified working 2026-08-09.

**⚠️ Disabled (`isDisabledByUser: true`) since 2026-08-09 — and decided (2026-08-10) to stay off deliberately**, not just paused during Studio experimentation. Per-edit auto-rebuild costs a 15-credit Netlify production deploy on every single Sanity save; manual redeploy after a batch of edits is more cost-effective and is now the intended workflow going forward (including post-launch). The hook itself is being left configured (not deleted) in case auto-rebuild is ever wanted again — re-enable by PATCHing `{"isDisabledByUser": false}` to `https://api.sanity.io/v2025-08-04/hooks/projects/wqa0no5g/IQSKUBSymrG2dmll` if that changes. Until/unless re-enabled, content edits in Sanity will NOT show up on the live site without a manual deploy (Netlify dashboard "Trigger deploy", or hitting the build hook URL directly) — Kerry/Shan will need this as their normal publish step; see "Kerry & Shan need access to" below.

**Gotcha if this ever needs recreating:** Sanity's webhook API (`POST /v2025-08-04/hooks/projects/{projectId}`) accepts a hook with `rule: null` without complaint, but a null rule silently matches nothing — the hook exists and looks fine in `sanity hooks list`, but never fires, with zero entries in `sanity hooks logs`. It needs an explicit rule set via `PATCH`: `{"rule": {"on": ["create", "update", "delete"], "filter": "true"}}`. Confirmed via the webhook's `attempts` endpoint (`GET /v2025-08-04/hooks/projects/{projectId}/{hookId}/attempts`) — that's the reliable way to check whether a webhook has actually ever fired, rather than trusting that "exists" means "works."

## Account setup checklist
Reasoning: if these accounts are created under the user's personal logins, the charity doesn't actually own its own site — a volunteer does, on their behalf. Creating them as org/team resources from the start avoids a painful migration later and gives Souper Troopers itself ultimate control.
- [x] **GitHub**: create a GitHub *Organization* (not a personal repo) — done, `souper-troopers/website`. Repo is public (made public 2026-08-08 — nothing sensitive in it, and Netlify's free tier can't deploy private org-owned repos or add team members without a paid plan, so public was the pragmatic choice).
- [x] **Netlify**: deploying under a Team ("Souper Troopers"), not the user's personal account. Chose Netlify over Vercel because Vercel's free tier doesn't support Teams at all (Pro-only, ~$20/mo). Deployed to a temporary `*.netlify.app` URL — **not yet pointed at the real domain** (soupertroopers.org still serves the current live site until this redesign is approved).
- [ ] **Sanity**: deferred — creating a Sanity Organization requires payment details upfront, so we're starting under the user's personal account instead to avoid blocking schema/content work. Sanity supports transferring a project to an Organization later; **revisit this before real launch.**
- [x] **Payment processor — PayFast direct integration, fully built and verified end-to-end in production** (2026-08-09): corrected from Paystack (not Stripe/Snipcart — see "Stack" above) once we found the current live site already runs on a working PayFast merchant account via WooCommerce, and confirmed PayFast isn't tied to WooCommerce at all — it has its own general "Custom Integration" API. Reused the existing sandbox account directly, no new signup/FICA wait. Built cart (`src/lib/cart.svelte.ts`, `CartWidget.svelte`) plus two Netlify Functions (`create-payfast-payment`, `payfast-itn`). **Two real bugs found and fixed via actual sandbox transactions, not just code review**: (1) `phpUrlEncode` didn't match PHP's `urlencode()` for `!'()*~` — any item name with a parenthesis broke the signature; (2) the bigger one — the ITN's signature must be verified against PayFast's **raw POST body as received** (including empty fields like `custom_str1=`, `name_first=`), not a reconstruction from parsed fields, which silently dropped every empty field and broke verification every time regardless of fix (1). Confirmed working with a real completed sandbox purchase 2026-08-09 16:47 ("Gift Tag — India Harris (pack of 5)", R85.00) — `payfast-itn` logged "PayFast payment confirmed". Full history in `docs/shop-checkout-decision.md`.
- [x] **Visual mockup built for the decision** (2026-08-09): a Claude Artifact comparing "Current" (request-by-email) vs "Proposed" (Paystack cart + checkout) side by side, using the real product/pricing data — so this is a real comparison to look at, not just prose. Faithfully recreates the live site's brand (Quicksand font embedded, exact color tokens from `Layout.astro`). Non-functional (no real cart/payment wiring) — purely for the decision conversation with Kerry. Private by default; needs sharing from the artifact's share menu before sending to her.
- [x] **Deploy the Sanity Studio**: done 2026-08-08, live at https://souper-troopers.sanity.studio/. Gated behind Sanity's own login — Kerry/Shan will need to be added as project members (Sanity project settings → Members) before they can actually access it.
- [ ] **Kerry & Shan need access to** (noted by user 2026-08-09, not yet granted): GitHub org (`souper-troopers`), Sanity project (Members, see above), Netlify team ("Souper Troopers"), and Cloudflare (for the Web Analytics dashboard added 2026-08-09 — either full dashboard access or at minimum a way to view the stats). Same ownership reasoning as the rest of this checklist — invite via each platform's own team/member settings rather than sharing the user's personal logins. **Netlify access needs a deep-link, not just an invite (added 2026-08-10)**: since the auto-rebuild webhook is staying off (see "Publish → rebuild pipeline" above), manual redeploy via the Netlify dashboard's Deploys page ("Trigger deploy") is now the actual publish step after a batch of Sanity edits — send Kerry/Shan the direct link to that page alongside the team invite, not just the invite itself.

## Before real launch (pointing soupertroopers.org at this site)
- [ ] Remove the `<meta name="robots" content="noindex, nofollow">` tag in `src/layouts/Layout.astro` — added 2026-08-08 so search engines don't index the pre-launch preview URL under the wrong domain.
- [ ] Point the real domain at Netlify and update DNS.
- [ ] Transfer the Sanity project from the user's personal account to a Sanity Organization.
- [ ] Update `site:` in `astro.config.mjs` from `souper-troopers.netlify.app` to the real domain (used for canonical/Open Graph URLs — see "SEO/AEO basics" below). Also remove the `Disallow: /` rule in `public/robots.txt` at the same time as the noindex meta tag above.
- [ ] Add a sitemap (`@astrojs/sitemap` integration) — deliberately skipped while the site is noindexed, since a sitemap is pointless until search engines are actually allowed to crawl it. Add once the noindex/robots.txt restrictions above are lifted.
- [ ] Add a new Cloudflare Web Analytics site/token for the real domain (the current one, added 2026-08-09, is scoped to `souper-troopers.netlify.app`) and swap it into the script tag in `Layout.astro` — see "Analytics" below.
- [ ] **Netlify Forms notification email**: confirm who/where the Contact form (and any future donor-facing forms) notification email is actually configured — it's a Netlify dashboard setting (Site configuration → Forms → Form notifications), not anything in code or Sanity, so nothing here shows it. Raised 2026-08-09 when the user realized it may never have been set up at all — check it's pointed at a real, monitored inbox now, and change it to whichever address should receive live submissions before launch (may differ from whatever it's set to during dev/testing).
- [x] **Sanity → Netlify auto-rebuild webhook: decided 2026-08-10, staying off.** Manual redeploy (after a batch of Sanity edits, via the Netlify dashboard) is the chosen ongoing workflow, not just a pre-launch stopgap — see "Publish → rebuild pipeline" above. The webhook itself is left configured, not deleted, in case this is ever revisited.

## Content still needed
- ~~Shop pricing & fulfillment~~ **Pricing resolved 2026-08-09** — see "WooCommerce integration" and "Payment processor" below: real prices/variants/photos now live in Sanity, and real PayFast checkout is live. **Fulfillment (stock/inventory tracking, who prepares orders) is still open** — no automated inventory exists (deliberate, see `docs/shop-checkout-decision.md`), collection stays manual (in-person or buyer-arranged courier) regardless. `OrderForm.svelte` (the old email-request flow) has been removed, replaced by the cart.
- **More media generally, plus placement recommendations** (broadened by user 2026-08-09 — was previously just the hero-video item below): waiting on Kerry/Shan for a wider set of photos/videos to work with beyond what's currently in the site, **and** their input on where specific pieces should actually go (homepage, team page, CAST journey, etc.) rather than us guessing placement. Noted as an open ask on the `request-for-comment` page.
  - **Video for the homepage hero specifically**: current hero uses a real photo (`dignity-moment.jpg`), not video. Two source videos exist in `../souper-troopers-media/Content/Videos/` but neither is hero-ready: `Capital International Group - CSI Partner.mov` is 933MB / 7 min (a narrated piece, better suited to a "watch our story" section than a silent autoplay background), and `Anthony - Cast Model.mp4` is 92MB / 75s but portrait-oriented. Both also exceed GitHub's 100MB file-size limit, so raw files can't be committed to this repo regardless. Needs either: short (5-15s), silent, heavily-compressed (~2-3MB) b-roll shot specifically for a hero background, or the existing long-form videos uploaded to YouTube/Vimeo (unlisted) and compressed, then embedded as a click-to-play "watch our story" section (not autoplay).

## Open questions / follow-ups (noted 2026-08-09)
**Needs Kerry's input specifically** (quick-scan list — see individual items below for full context): Shmiley domain purpose, EFT bank account details, which donation channel is most used. (WordPress/WooCommerce access and the checkout build-vs-manual decision are now resolved/moot — see "WooCommerce integration" and "Payment processor" below.)

**Kerry should be briefed on** (things to *tell* her, not ask her — quick-scan list): she and Shan need inviting to GitHub/Sanity/Netlify/Cloudflare (see Account setup checklist above); Cloudflare Web Analytics doesn't update in real time (see "Analytics" below); the new Sanity dataset backup system now exists (private `website-backups` repo, weekly export — see "Working agreement" above), including that it contains personal data so access to that repo should stay limited.

- [x] **Sanity backup automation — secrets set, code committed & pushed to `dev`** (2026-08-09): `SANITY_API_TOKEN` (Viewer/read-only) and `BACKUP_REPO_TOKEN` (fine-grained PAT, write-scoped to `website-backups` only) are both set on `souper-troopers/website`. **Also confirmed: `StephenSPaul` doesn't have admin rights on the `website` repo either** (same permission gap as the earlier repo-creation issue) — setting secrets there needed switching to `Memeplexx`. Worth deciding who actually administers org repos going forward rather than hitting this every time.
- [x] **Sanity backup automation — fully verified working end-to-end** (2026-08-09): merged `dev` → `main` twice (user approved both, two Netlify credits spent) to test via `workflow_dispatch`. First run failed (`--project` isn't a real flag, fixed to `--project-id`); second run succeeded — confirmed `production-2026-08-09.tar.gz` actually landed in `souper-troopers/website-backups/sanity/`. Weekly schedule (Sundays 03:00 UTC) is now live on `main`. Dispatching/re-testing this workflow needs the `Memeplexx` account — `StephenSPaul` lacks admin rights to do so, same pattern as secrets/repo-creation.

- [x] **Social links**: added as a footer-only icon row under "Follow us" in `src/layouts/Layout.astro`, hardcoded (agreed with user 2026-08-09 — same rationale as the hardcoded nav links: rarely-changing, not worth a Sanity round-trip). URLs pulled from the live old site (soupertroopers.org) HTML: Facebook `facebook.com/soupertroopers`, Instagram `instagram.com/souper_troopers`, LinkedIn `linkedin.com/company/souper-troopers-npo`, TikTok `tiktok.com/@souper_troopers`, YouTube `youtube.com/@SouperTroopers1818`.
- [ ] **Shmiley domain — now flagged as directly relevant to shop work** (raised again by user 2026-08-09, while mid-scoping the PayFast checkout build): Kerry mentioned buying a domain for "Shmiley" — purpose still unclear, but Shmiley (Pty) Ltd is literally the entity running the shop (Troopers Coffee, African Worry Dolls), so there's a real chance she's picturing the shop as its own separate domain/site rather than a section of the main site. Doesn't block scoping/building now (Sanity content and the PayFast integration code stay portable regardless of final domain), but **needs to be asked before shop work goes live** — bundle with the other shop questions for Kerry (WordPress/WooCommerce access, the checkout build-vs-manual decision).
  - **Candidate technical answer, if she does want a separate Shmiley domain** (discussed 2026-08-09, not yet built — only worth pursuing once her actual intent is confirmed): three options were weighed —
    1. **Recommended**: a second, small, standalone static site for the Shmiley domain (just a landing page + the shop), built from the *same* components/design/Sanity content as the main site — no new stack, no server needed, least new infrastructure. This is the one to lead with if she confirms she wants a separate presence.
    2. Point both domains at the exact same deployed site — simplest possible, but both show the *identical* full site, not really "the shop has its own presence."
    3. One app serving different content per domain at request time — technically possible but needs switching from static to server-rendered, a real stack change (contradicts the "Hosting" line in "Stack" above) — not worth it for what's being described here.
  - Framed non-technically for Kerry on the `request-for-comment` page (see below) — not exposing this level of detail to her, just that it's "very achievable, reuses everything, no rebuild from scratch."
- [x] **WooCommerce integration — superseded by a one-time migration into Sanity, WooCommerce dependency now fully removed** (2026-08-09): the original plan (this line, until today) was ongoing WooCommerce API dependency as the ever-live source of truth. That got revisited later the same day when checkout came up — see "Payment processor" above — and the final shape is better: real product/variant/pricing data (and real photos) were pulled from the live WooCommerce site **once**, via its embedded variation JSON (`data-product_variations`), and migrated into new Sanity types `productCategory` + `shopItem` (replaced the old `product` type entirely — see `studio/schemaTypes/`). Sanity is now the sole, permanent source of truth for shop content; the old WordPress/WooCommerce site could be fully retired without breaking anything here. Solves the "Shop pricing & fulfillment" gap under "Content still needed" above with real, confirmed data — 3 categories, 11 items, all real prices/photos. Background reasoning in `docs/woocommerce-product-data-decision.md` (now historical — written before this pivot, kept for context on why WooCommerce isn't a live dependency).
  - ~~Needs from Kerry: WordPress login...~~ **No longer needed** — turned out unnecessary once we pivoted to the one-time-migration approach. Real product/variant/price data (and photos) were pulled straight from the live site's *public* product pages (their embedded `data-product_variations` JSON, no login required), so WordPress/WooCommerce access was never actually used and isn't needed going forward either. Remove this from anything sent to Kerry.
  - Checkout (how payment actually happens) is a separate, still-open question from product *data* — not decided here.
- [x] **Shop page form has no CAPTCHA**: fixed as part of the Netlify Forms rollout below — `OrderForm.svelte` now submits to Netlify Forms (with a honeypot field) instead of building a `mailto:` link.
- [x] **Contact us form**: added to `src/pages/contact.astro` — name/email/message, Netlify Forms + honeypot, same pattern as the shop form.
- [x] **Analytics**: **Cloudflare Web Analytics** live in `src/layouts/Layout.astro` with the user's real site token (added to their Cloudflare account for `souper-troopers.netlify.app`, 2026-08-09). Chosen over GA4 (heavier/cookie-based) and Plausible (no free tier). **Note for the future domain-launch checklist**: Cloudflare Web Analytics tracks per registered hostname — when the real domain goes live, a *new* site/token likely needs to be added in the Cloudflare dashboard for that hostname (the current token is scoped to the Netlify preview URL), then swapped into this script tag. **Remember to tell Kerry & Shan (2026-08-09, when granting their Cloudflare access above) that the dashboard doesn't update in real time** — Cloudflare Web Analytics data typically lags, so a visit made just now won't necessarily show up immediately; avoid them thinking it's broken.
- [x] **SEO/AEO basics** (2026-08-09): added canonical URLs, Open Graph + Twitter Card meta tags (using `dignity-moment.jpg` as the default share image), JSON-LD structured data (`schema.org NGO` type, in `Layout.astro`, populated from Sanity `siteSettings`), `public/robots.txt` (currently `Disallow: /` to match the existing noindex meta tag), and `public/llms.txt` for AI-agent/answer-engine discoverability. `site:` in `astro.config.mjs` is set to `https://souper-troopers.netlify.app` (confirmed live 2026-08-09) — canonical/OG/JSON-LD URLs all resolve correctly now. Sitemap deliberately deferred — see "Before real launch" above.
- [x] **Social links display**: old site (soupertroopers.org) showed a subset (Facebook, LinkedIn, Instagram) as icons in the header nav, plus the full set (+ YouTube, TikTok) as an icon row under a "Follow us" heading in the footer. Implemented footer-only (see "Social links" above).
- [x] **Donate page — payment provider icons**: added a small SVG icon to each "Ways to give" card in `src/pages/donate.astro`. PayPal uses a recognizable brand-glyph path in PayPal blue; EFT, SnapScan, and Monthly giving use generic representative icons (bank, QR code, recurring-arrows) rather than exact official logos, since no verified/official SnapScan or PaySoftImpact logo asset was sourced — hand-recreating an exact brand mark from memory risked misrepresenting it. If real official logo files (PNG/SVG) are ever supplied — e.g. from snapscan.io's brand/press page — swap them in `src/assets/images/` in place of the SVGs.
- [x] **Monthly giving — external redirect notice**: added "You'll be securely redirected to PaySoftImpact to set up your monthly gift" under the Give Monthly button in `donate.astro`, since it's a fully external hosted checkout we don't control the UI of — this at least sets the expectation before the click-through.
- [ ] **Which donation channel is most used?** (EFT / SnapScan / PayPal / PaySoftImpact monthly) — open question from the Notion discovery doc, still unanswered. Worth asking Kerry directly: orgs often nominally support many channels but get the bulk of volume through just one. Proposal once answered: feature/order the "Ways to give" cards on `donate.astro` by actual usage (most-used first or visually emphasized) rather than the current arbitrary EFT/SnapScan/PayPal/Monthly order.
- [ ] **EFT bank account details missing**: the Direct EFT card on `donate.astro` says "email your proof of payment" but never actually shows the account holder name, bank, branch code, or account number — same gap as the old site (per the Notion discovery doc: "EFT bank details — not shown on the current site; needed if EFT is to be presented on the new site"). Need these from Kerry/Shan before the card is actually usable for a first-time donor.
- [ ] **Section 18A certificate issuance — currently fully manual**: donor emails proof of payment, staff manually compose a certificate. Possible improvement (not yet built, needs a decision first): replace the "email us" instruction with a proper form (name, amount, date, tax number) that auto-generates the PDF from a template — cuts staff effort from composing to reviewing/sending. Full automation (payment confirmation → certificate sent with no human step) is realistic only for the electronic rails (SnapScan/PayPal/PaySoftImpact, which confirm payment programmatically) not EFT (needs bank reconciliation, out of scope). Any of this needs a small backend/serverless function — this site is currently pure static, so it's a real stack-scope decision, not a quick add. Any donor-facing form built for this needs the same CAPTCHA treatment as the shop and contact forms above — worth deciding spam protection once, across all three forms, rather than per-form.
- [x] **Goods donation tiles (get-involved.astro)**: restored the "email manager@soupertroopers.org to arrange" line (was on the old site, missing here) and made each goods-tile a `mailto:manager@soupertroopers.org?subject=Donation drop-off: {category}` link. **Treated as an interim fix, not the real answer** — see "Forms should move off mailto onto real backend" below, which the user raised immediately after this.
- [x] **Forms moved off mailto onto Netlify Forms** (raised by user 2026-08-09, implemented same day): built the Shop order-request form (`OrderForm.svelte`) and the new Contact form (`contact.astro`) on Netlify Forms instead of `mailto:` — plain HTML `<form data-netlify="true">` submission via `fetch`, with a honeypot field (`bot-field`) on each for spam filtering, no backend code, no new dependency. Since `OrderForm.svelte` is a Svelte island (hydrated client-side), also added a hidden static duplicate of the form in `shop.astro` itself — Netlify detects form names by scanning the *built* HTML at deploy time, and a plain Astro-rendered duplicate guarantees detection regardless of how the island hydrates.
  - **Confirmed fully working 2026-08-09** (re-verified with a real submission after all the subsequent deploys for the Sanity backup and shop/checkout work): originally 404'd because "Form detection" (Site configuration → Build & deploy → Post processing) was off; user enabled it, and the next fresh deploy picked it up. A live test POST of `form-name=contact` now returns `200`. (Note: `order-request` no longer exists as a Netlify Form — that form was removed entirely when the shop cart replaced it, see "WooCommerce integration"/"Payment processor" above.)
  - The goods-donation tiles (get-involved.astro) and Section 18A "email us" flow are still mailto — deliberately left as-is for now; revisit once the Netlify Forms pattern above is confirmed working live.
- [ ] **Suggested donation amounts**: considered adding rand-amount quick-pick tiles (e.g. "R150 / R350 / R500 / R1000") tied to specific funded items (transport, course fees, ID documents, care packs), a pattern seen on comparable charity sites. Shelved (user decision 2026-08-09) — unconfirmed whether donations are actually earmarked to specific costs (vs. pooled into general funds), so a specific "R150 = transport" claim could be inaccurate/misleading. **Worth raising with Kerry as a suggestion** rather than building speculatively — if she confirms real per-item costs, the safer version (amount quick-picks alone, no specific-outcome claim) could go in quickly.

## Source of truth for requirements
Website scope/requirements come from the "🌐 Souper Troopers — Website Discovery" Notion database, not from files in this repo. Access via the Notion MCP connection (see `.mcp.json`). Check it for current answered/open questions before assuming scope is settled.

## Repo layout
- `docs/` — planning docs: site structure & visitor journeys, client-facing proposal, design-inspiration notes.
- `wireframes/` — early static HTML wireframes (reference only; superseded by the real Astro build as pages land).
- `src/` — the actual Astro/Svelte site.
- Media assets (photos, videos, logos, brand PDFs, product one-pagers) live in a sibling directory, `../souper-troopers-media/`, not in this repo.

## Development
**`npm run dev` (repo root) starts all three dev processes together** via `concurrently`, labeled/color-coded in one output stream — Ctrl+C stops all three: the Astro site (:4321), the Sanity Studio (:3333), and a `netlify dev` proxy (:8888) that serves the local `netlify/functions/` (`create-payfast-payment`, `payfast-itn`) on top of the Astro site.

**Browse the site at `http://localhost:8888`, not `:4321`, during local dev.** `:4321` is the raw Astro server — it has no idea `netlify/functions/` exists, so anything that calls `/.netlify/functions/*` (e.g. the Shop checkout) 404s there. `:8888` is `netlify dev`'s proxy: same site, same content, but with working functions layered on top. (Previously these were two separate scripts, `dev` and `dev:functions` — merged 2026-08-09 after this split caused real confusion: browsing the default `npm run dev` port and hitting a function-not-found error on Shop checkout. No real downside to merging — `netlify dev` here reads secrets from the local `.env`, not a linked Netlify account, so it doesn't add any auth requirement.)

**Gotcha (why the `dev` script is built the way it is)**: `netlify dev`'s normal framework auto-detection is not usable in this project — running it plain hits an interactive "Multiple possible dev commands found" prompt (Astro vs. Svelte both resolve to `npm run dev`), which hangs forever in a non-interactive/agent session. Passing `--command "npx astro dev"` directly doesn't work either — Astro's dev server daemonizes itself immediately (prints "Dev server running" and returns control), which `netlify dev` misreads as the framework process crashing, so it shuts itself down right after starting. The fix baked into `package.json`: `netlify dev` is given a no-op long-running placeholder command (`--command "tail -f /dev/null"`) alongside `--target-port 4321` — it just needs to see port 4321 already answering (which `concurrently` starts in parallel) and proxies to it; what its own supervised "command" does doesn't matter as long as it doesn't exit.

Reads `.env` automatically for local secrets (`PAYFAST_MERCHANT_ID` etc.). `URL` is auto-injected by `netlify dev` to match its own local address, so PayFast's `return_url`/`notify_url` correctly point at `localhost:8888` during local testing without any extra config.

When starting it in the background (e.g. so an agent session isn't blocked on it), run the underlying processes directly rather than through the unified `npm run dev` — `astro dev --background` for Astro (managed with `astro dev stop`, `astro dev status`, `astro dev logs`), `sanity dev --port 3333` from `studio/` for the Studio (backgrounded via `nohup ... &`, stopped via `pkill -f "sanity dev"` — no background-mode flag of its own), and `netlify dev --command 'tail -f /dev/null' --target-port 4321 --no-open` for functions (same `pkill -f "netlify dev"` pattern) once Astro is confirmed up.

## Documentation
Full documentation: https://docs.astro.build

Consult these guides before working on related tasks:
- [Adding pages, dynamic routes, or middleware](https://docs.astro.build/en/guides/routing/)
- [Working with Astro components](https://docs.astro.build/en/basics/astro-components/)
- [Using React, Vue, Svelte, or other framework components](https://docs.astro.build/en/guides/framework-components/)
- [Adding or managing content](https://docs.astro.build/en/guides/content-collections/)
- [Adding styles or using Tailwind](https://docs.astro.build/en/guides/styling/)
- [Supporting multiple languages](https://docs.astro.build/en/guides/internationalization/)
