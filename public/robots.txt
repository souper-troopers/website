# Pre-launch preview — this mirrors the <meta name="robots" content="noindex, nofollow">
# tag in src/layouts/Layout.astro. Remove this Disallow rule at the same time that meta
# tag is removed (see "Before real launch" in AGENTS.md).
User-agent: *
Disallow: /

# Generated by @astrojs/sitemap at build time. The URLs inside are built from `site:` in
# astro.config.mjs, so this points at the preview domain until that is switched over.
Sitemap: https://souper-troopers.netlify.app/sitemap-index.xml
